apiVersion: v1
kind: Service
metadata:
  name: ci-cd-pipeline
  namespace: python-ci-cd
  labels:
    app: ci-cd-pipeline
    version: v1
spec:
  selector:
    app: ci-cd-pipeline
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP
